_*
*.pyc
**/__pycache__
.env
*.code-workspace
autoryzowane*
authorized*
*.pickle
.vscode
target
secrets*
heating_chart.json
.pio
credentials*
# ...but the mesh firmware's credentials module is source code, not secrets:
# it is the thing that reads the values out of NVS at boot. Likewise the
# provisioning template, which ships placeholders only.
!uc/buttonsMeshIDF/src/credentials.c
!uc/buttonsMeshIDF/src/credentials.h
!uc/buttonsMeshIDF/provisioning/credentials.csv.example
kiczora
chrobacz
turbacz_app
turbacz/wifiLightButton/plain_passwords
connections.json
# Uploaded OTA images. They embed WiFi/MQTT credentials and are served only
# through the authenticated /firmware/<device>.bin route -- never commit them.
turbacz/firmware/
*.bin
*.egg-info
turbacz.toml
.DS_STORE
# generated by turbacz/setup.sh, contains hashed broker credentials
mosquitto.passwd

# Home Assistant runtime state. .storage holds auth tokens and the MQTT
# password -- never commit it. Only the seeded config files are tracked.
turbacz/homeassistant/*
!turbacz/homeassistant/configuration.yaml
!turbacz/homeassistant/automations.yaml
!turbacz/homeassistant/scripts.yaml
!turbacz/homeassistant/scenes.yaml
