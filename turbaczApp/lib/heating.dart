import 'package:flutter/material.dart';

class Heating extends StatelessWidget {
  const Heating({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Heating Page'),
    );
  }
}
